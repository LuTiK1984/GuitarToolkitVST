<UserControl x:Class="GuitarToolkit.UI.ToolkitHostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GuitarToolkit.UI"
             Background="{DynamicResource AppBackgroundBrush}"
             Loaded="UserControl_Loaded"
             MinWidth="750" MinHeight="500">

    <UserControl.Resources>
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#1A1525"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#241B33"/>
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#352847"/>
        <SolidColorBrush x:Key="ControlBrush" Color="#4A3860"/>
        <SolidColorBrush x:Key="ControlHoverBrush" Color="#5B4676"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#CBA6F7"/>
        <SolidColorBrush x:Key="AccentGreenBrush" Color="#A6E3A1"/>
        <SolidColorBrush x:Key="StartBrush" Color="#A6E3A1"/>
        <SolidColorBrush x:Key="GoodBrush" Color="#A6E3A1"/>
        <SolidColorBrush x:Key="WarnBrush" Color="#F9E2AF"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#F38BA8"/>
        <SolidColorBrush x:Key="FavBrush" Color="#F9E2AF"/>
        <SolidColorBrush x:Key="TextBrush" Color="#CDD6F4"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#9B8BB8"/>
        <SolidColorBrush x:Key="DarkBrush" Color="#1A1525"/>
        <SolidColorBrush x:Key="TabBrush" Color="#2D2240"/>
        <SolidColorBrush x:Key="TabHoverBrush" Color="#4A3860"/>
        <SolidColorBrush x:Key="TabSelectedBrush" Color="#CBA6F7"/>
        <SolidColorBrush x:Key="TabSelectedTextBrush" Color="#1A1525"/>

        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="Chrome"
                                Background="{DynamicResource TabBrush}"
                                Padding="14 10"
                                Margin="2"
                                CornerRadius="6">
                            <TextBlock Text="{TemplateBinding Header}"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontSize="{TemplateBinding FontSize}"
                                       FontWeight="{TemplateBinding FontWeight}"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource TabHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource TabSelectedBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TabSelectedTextBrush}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource TabSelectedBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TabSelectedTextBrush}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>

        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{DynamicResource ControlBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource DarkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="ComboToggle" TargetType="{x:Type ToggleButton}">
            <Border x:Name="Chrome"
                    Background="{DynamicResource ControlBrush}"
                    CornerRadius="6"
                    Padding="10 6"
                    BorderThickness="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="20"/>
                    </Grid.ColumnDefinitions>
                    <ContentPresenter Grid.Column="0"
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1"
                               Text="v"
                               FontSize="12"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextBrush}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="{x:Type ComboBox}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton Template="{StaticResource ComboToggle}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False"
                                          ClickMode="Press"/>
                            <ContentPresenter IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="12 6 28 6"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="{DynamicResource PanelBrush}"
                                        CornerRadius="6"
                                        BorderBrush="{DynamicResource PanelBorderBrush}"
                                        BorderThickness="1"
                                        Padding="4"
                                        Margin="0 4 0 0">
                                    <ScrollViewer MaxHeight="300">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Padding" Value="10 7"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="{x:Type Slider}">
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
        </Style>

        <Style TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>

        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
    </UserControl.Resources>

    <TabControl x:Name="ToolkitTabs"
                Background="{DynamicResource AppBackgroundBrush}"
                BorderThickness="0"
                TabStripPlacement="Left">

        <TabItem Header="&#x1F3B8; &#x422;&#x44E;&#x43D;&#x435;&#x440;">
            <ScrollViewer>
                <local:TunerView x:Name="TunerTab"/>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="&#x1F941; &#x41C;&#x435;&#x442;&#x440;&#x43E;&#x43D;&#x43E;&#x43C;">
            <local:MetronomeView x:Name="MetronomeTab"/>
        </TabItem>

        <TabItem Header="&#x1F3B5; &#x410;&#x43A;&#x43A;&#x43E;&#x440;&#x434;&#x44B;">
            <ScrollViewer>
                <local:ChordView x:Name="ChordTab"/>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="&#x1F3BC; &#x413;&#x430;&#x43C;&#x43C;&#x44B;">
            <local:FretboardView x:Name="FretboardTab"/>
        </TabItem>

        <TabItem Header="&#x1F3A7; &#x418;&#x43D;&#x442;&#x435;&#x440;&#x432;&#x430;&#x43B;&#x44B;">
            <ScrollViewer>
                <local:IntervalTrainerView x:Name="IntervalTab"/>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="&#x1F3B6; &#x41F;&#x440;&#x43E;&#x433;&#x440;&#x435;&#x441;&#x441;&#x438;&#x438;">
            <ScrollViewer>
                <local:ProgressionView x:Name="ProgressionTab"/>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="&#x25CB; &#x41A;&#x440;&#x443;&#x433; &#x43A;&#x432;&#x438;&#x43D;&#x442;">
            <local:CircleOfFifthsView x:Name="CircleTab"/>
        </TabItem>

        <TabItem x:Name="TabPlayerTabItem"
                 Header="&#x1F4C4; &#x422;&#x430;&#x431;&#x44B;"/>

        <TabItem Header="&#x2699; &#x41D;&#x430;&#x441;&#x442;&#x440;&#x43E;&#x439;&#x43A;&#x438;">
            <local:SettingsView x:Name="SettingsTab"/>
        </TabItem>

        <TabItem Header="&#x24D8; &#x41E; &#x43F;&#x440;&#x43E;&#x435;&#x43A;&#x442;&#x435;">
            <local:AboutView x:Name="AboutTab"/>
        </TabItem>
    </TabControl>
</UserControl>

